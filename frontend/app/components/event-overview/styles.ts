import { StyleSheet } from 'react-native';

export const overviewStyles = StyleSheet.create({
    container: {
        alignItems: 'center',
        height: '100%',
        padding: 10
    },
    title: {
        fontSize: 40,
        fontWeight: 'bold'
    }
})